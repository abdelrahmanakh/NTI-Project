[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "gemini-rag"
version = "0.1.0"
description = "Gemini-powered RAG system"
requires-python = ">=3.12,<3.13"

[tool.setuptools.packages.find]
include = ["src*"]